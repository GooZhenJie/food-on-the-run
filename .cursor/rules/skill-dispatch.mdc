---
description: Before implementing any change in FOTR, classify the task and load the matching skill first.
alwaysApply: true
---

# Skill Dispatch — Food on the Run

This is a **pnpm + Turborepo monorepo** with three apps:
- `apps/web` — customer-facing frontend (UmiJS + React + TypeScript + Tailwind + antd)
- `apps/admin` — internal back-office console (UmiJS + React + TypeScript + Tailwind + antd)
- `apps/server` — single Go + PostgreSQL backend serving both frontends

Before writing any code, follow the steps below.

## Step 1 — Classify the Task

| What you are about to do | Skill to load |
|---|---|
| Create / modify / delete a React **component** or **page** under `apps/web/src/` | `component-conventions` **+** `ui-design-system` |
| Create / modify / delete a React **component** or **page** under `apps/admin/src/` | `component-conventions` |
| Restyle, redesign, or add **user-facing UI** under `apps/web/src/` | `ui-design-system` |
| Add, rename, move, or delete any **file or folder** under `apps/web/src/` | `directory-structure` |
| Add, rename, move, or delete any **file or folder** under `apps/admin/src/` | `directory-structure-admin` |
| Add or modify an **API service function** under `apps/web/src/services/` or `apps/admin/src/services/` | `web-api-service` |
| Create / modify a **DB migration** (`.sql`), table, column, index, enum, or Go struct mapped to DB | `postgresql-naming-conventions` |
| Add or modify an HTTP **handler**, route, middleware, or any Go file under `apps/server/` | `go-server-conventions` |

## Step 2 — Load the Skill Before Writing Code

Use the `Read` tool on the exact path listed below. **Do not skip this step.**

| Skill | Path |
|---|---|
| `component-conventions` | `.cursor/skills/component-conventions/SKILL.md` |
| `ui-design-system` | `.cursor/skills/ui-design-system/SKILL.md` |
| `directory-structure` | `.cursor/skills/directory-structure/SKILL.md` |
| `directory-structure-admin` | `.cursor/skills/directory-structure-admin/SKILL.md` |
| `web-api-service` | `.cursor/skills/web-api-service/SKILL.md` |
| `postgresql-naming-conventions` | `.cursor/skills/postgresql-naming-conventions/SKILL.md` |
| `go-server-conventions` | `.cursor/skills/go-server-conventions/SKILL.md` |

## Step 3 — Implement

Apply every constraint in the skill without exception.

> When a task touches multiple categories (e.g. a new end-to-end feature spanning admin UI + server), load **all** matching skills before writing any file.

## Step 4 — Post-Action

After completing the implementation, run any required follow-up:

| What changed | Required action |
|---|---|
| Any `.sql` migration file under `db/migrations/` | `cd apps/server && make migrate-up` **and** update matching `docs/api/consumer/*.md` and/or `docs/api/admin/*.md` in the same PR |
| Any SQL query file under `db/queries/` | `cd apps/server && sqlc generate` |
| New consumer Go handler added | Verify route is registered under `/api/*` in `registerRoutes` in `main.go` |
| New admin Go handler added | Verify route is registered under `/api/admin/*` and the `RequireAdmin` + `Audit` middleware chain applies |
| New component created in `apps/web/` | Verify against the `component-conventions` **and** `ui-design-system` checklists |
| New component created in `apps/admin/` | Verify against the `component-conventions` **and** `directory-structure-admin` checklists |
| New consumer page added | Verify against the `ui-design-system` New Page checklist (pill primary button, orange brand color, Inter font, mobile-first) |
| New admin page added | Verify against the `directory-structure-admin` New Module checklist (uses `DataTable` / `FormDrawer` primitives, route registered in `apps/admin/.umirc.ts`) |
| Mock route removed | Confirm matching service function in `src/services/` is wired up |

---

> **Maintaining this file:** every new skill added to `.cursor/skills/` must also get a row in the Step 1 table and the Step 2 table above, otherwise it will never be triggered.
